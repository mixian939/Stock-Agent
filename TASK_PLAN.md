# 当前修改计划

更新时间: 2026-04-16

## 目标

1. 扩充 ETF 池数量，但保持现有 7 类资产暴露不变。
2. 新增本地 LM Studio 金融子 agent，并接入现有主 agent 的最终决策流程。
3. 让主 agent 以 60% 子 agent + 40% 主 agent 的方式做加权评分并输出最终调仓结论。
4. 适配前端页面，展示双 agent 决策信息。
5. 完成测试并更新 `PROJECT_REPORT.md`，补充数据接口来源说明。

## 执行步骤

### 1. 现状梳理

- 已完成：确认当前数据抓取、回测、日志、API、前端页面结构。
- 已完成：确认主 agent 为 `gpt-5.4`，项目已存在自主调仓流程。
- 已完成：确认数据层为 `Tushare` 优先、`jqdatasdk` 兜底。

### 2. ETF 池扩容

- 已完成：ETF 池扩充到 14 只 ETF。
- 已完成：通过“类别代表”机制保持资产类别仍为现有 7 类。
- 已完成：同步更新代码映射、名称和项目报告。

### 3. 双 Agent 决策

- 已完成：新增本地金融子 agent 配置，当前默认参数为用户指定的 `http://127.0.0.1:11434/v1` + `fin-r1`。
- 已完成：实现“子 agent 给建议 -> 主 agent 60/40 加权评分 -> 最终执行”的协调层。
- 已完成：在不大改原架构的前提下，把最终决策写入现有日志体系。

### 4. 后端与前端适配

- 已完成：扩展 API，返回子 agent 建议、主 agent 观点、加权评分和最终结果。
- 已完成：调整首页 / 对比页 / 聊天页展示双 agent 决策结构。
- 已完成：前端 `lint` 和 `build` 验证通过。

### 5. 测试与报告

- 已完成：运行单元测试，`32 passed`。
- 已完成：运行 1 个月双 agent smoke test，确认本地子 agent 已实际生效。
- 已完成：运行扩容后的 Headless 全流程回测，确认策略主流程正常。
- 已完成：更新 `PROJECT_REPORT.md`。
- 已完成：在 `PROJECT_REPORT.md` 末尾明确写出数据最终通过 `joinquant/jqdatasdk` 获取到。

## 当前注意事项

- 不覆盖你已有的未提交改动。
- 尽量不改数据获取、保存、日志保存的大框架。
- 所有新增逻辑优先复用现有 `logger`、`state`、`API`、`frontend` 结构。
- 当前本地金融子 agent 已切换为用户指定配置；后续若更换本地模型，只需改环境变量或 `config.py` 默认值。
